// generated from rosidl_typesupport_fastrtps_cpp/resource/idl__type_support.cpp.em
// with input from ros_etsi_its_msgs:msg/OriginatingVehicleContainer.idl
// generated code does not contain a copyright notice
#include "ros_etsi_its_msgs/msg/detail/originating_vehicle_container__rosidl_typesupport_fastrtps_cpp.hpp"
#include "ros_etsi_its_msgs/msg/detail/originating_vehicle_container__functions.h"
#include "ros_etsi_its_msgs/msg/detail/originating_vehicle_container__struct.hpp"

#include <cstddef>
#include <limits>
#include <stdexcept>
#include <string>
#include "rosidl_typesupport_cpp/message_type_support.hpp"
#include "rosidl_typesupport_fastrtps_cpp/identifier.hpp"
#include "rosidl_typesupport_fastrtps_cpp/message_type_support.h"
#include "rosidl_typesupport_fastrtps_cpp/message_type_support_decl.hpp"
#include "rosidl_typesupport_fastrtps_cpp/serialization_helpers.hpp"
#include "rosidl_typesupport_fastrtps_cpp/wstring_conversion.hpp"
#include "fastcdr/Cdr.h"


// forward declaration of message dependencies and their conversion functions
namespace ros_etsi_its_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const ros_etsi_its_msgs::msg::Heading &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  ros_etsi_its_msgs::msg::Heading &);
size_t get_serialized_size(
  const ros_etsi_its_msgs::msg::Heading &,
  size_t current_alignment);
size_t
max_serialized_size_Heading(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
bool cdr_serialize_key(
  const ros_etsi_its_msgs::msg::Heading &,
  eprosima::fastcdr::Cdr &);
size_t get_serialized_size_key(
  const ros_etsi_its_msgs::msg::Heading &,
  size_t current_alignment);
size_t
max_serialized_size_key_Heading(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace ros_etsi_its_msgs

namespace ros_etsi_its_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const ros_etsi_its_msgs::msg::Speed &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  ros_etsi_its_msgs::msg::Speed &);
size_t get_serialized_size(
  const ros_etsi_its_msgs::msg::Speed &,
  size_t current_alignment);
size_t
max_serialized_size_Speed(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
bool cdr_serialize_key(
  const ros_etsi_its_msgs::msg::Speed &,
  eprosima::fastcdr::Cdr &);
size_t get_serialized_size_key(
  const ros_etsi_its_msgs::msg::Speed &,
  size_t current_alignment);
size_t
max_serialized_size_key_Speed(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace ros_etsi_its_msgs

namespace ros_etsi_its_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const ros_etsi_its_msgs::msg::WGS84Angle &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  ros_etsi_its_msgs::msg::WGS84Angle &);
size_t get_serialized_size(
  const ros_etsi_its_msgs::msg::WGS84Angle &,
  size_t current_alignment);
size_t
max_serialized_size_WGS84Angle(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
bool cdr_serialize_key(
  const ros_etsi_its_msgs::msg::WGS84Angle &,
  eprosima::fastcdr::Cdr &);
size_t get_serialized_size_key(
  const ros_etsi_its_msgs::msg::WGS84Angle &,
  size_t current_alignment);
size_t
max_serialized_size_key_WGS84Angle(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace ros_etsi_its_msgs

namespace ros_etsi_its_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const ros_etsi_its_msgs::msg::DriveDirection &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  ros_etsi_its_msgs::msg::DriveDirection &);
size_t get_serialized_size(
  const ros_etsi_its_msgs::msg::DriveDirection &,
  size_t current_alignment);
size_t
max_serialized_size_DriveDirection(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
bool cdr_serialize_key(
  const ros_etsi_its_msgs::msg::DriveDirection &,
  eprosima::fastcdr::Cdr &);
size_t get_serialized_size_key(
  const ros_etsi_its_msgs::msg::DriveDirection &,
  size_t current_alignment);
size_t
max_serialized_size_key_DriveDirection(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace ros_etsi_its_msgs

namespace ros_etsi_its_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const ros_etsi_its_msgs::msg::VehicleLength &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  ros_etsi_its_msgs::msg::VehicleLength &);
size_t get_serialized_size(
  const ros_etsi_its_msgs::msg::VehicleLength &,
  size_t current_alignment);
size_t
max_serialized_size_VehicleLength(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
bool cdr_serialize_key(
  const ros_etsi_its_msgs::msg::VehicleLength &,
  eprosima::fastcdr::Cdr &);
size_t get_serialized_size_key(
  const ros_etsi_its_msgs::msg::VehicleLength &,
  size_t current_alignment);
size_t
max_serialized_size_key_VehicleLength(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace ros_etsi_its_msgs

namespace ros_etsi_its_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const ros_etsi_its_msgs::msg::VehicleWidth &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  ros_etsi_its_msgs::msg::VehicleWidth &);
size_t get_serialized_size(
  const ros_etsi_its_msgs::msg::VehicleWidth &,
  size_t current_alignment);
size_t
max_serialized_size_VehicleWidth(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
bool cdr_serialize_key(
  const ros_etsi_its_msgs::msg::VehicleWidth &,
  eprosima::fastcdr::Cdr &);
size_t get_serialized_size_key(
  const ros_etsi_its_msgs::msg::VehicleWidth &,
  size_t current_alignment);
size_t
max_serialized_size_key_VehicleWidth(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace ros_etsi_its_msgs

namespace ros_etsi_its_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const ros_etsi_its_msgs::msg::LongitudinalAcceleration &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  ros_etsi_its_msgs::msg::LongitudinalAcceleration &);
size_t get_serialized_size(
  const ros_etsi_its_msgs::msg::LongitudinalAcceleration &,
  size_t current_alignment);
size_t
max_serialized_size_LongitudinalAcceleration(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
bool cdr_serialize_key(
  const ros_etsi_its_msgs::msg::LongitudinalAcceleration &,
  eprosima::fastcdr::Cdr &);
size_t get_serialized_size_key(
  const ros_etsi_its_msgs::msg::LongitudinalAcceleration &,
  size_t current_alignment);
size_t
max_serialized_size_key_LongitudinalAcceleration(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace ros_etsi_its_msgs

namespace ros_etsi_its_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const ros_etsi_its_msgs::msg::YawRate &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  ros_etsi_its_msgs::msg::YawRate &);
size_t get_serialized_size(
  const ros_etsi_its_msgs::msg::YawRate &,
  size_t current_alignment);
size_t
max_serialized_size_YawRate(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
bool cdr_serialize_key(
  const ros_etsi_its_msgs::msg::YawRate &,
  eprosima::fastcdr::Cdr &);
size_t get_serialized_size_key(
  const ros_etsi_its_msgs::msg::YawRate &,
  size_t current_alignment);
size_t
max_serialized_size_key_YawRate(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace ros_etsi_its_msgs


namespace ros_etsi_its_msgs
{

namespace msg
{

namespace typesupport_fastrtps_cpp
{


bool
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_PUBLIC_ros_etsi_its_msgs
cdr_serialize(
  const ros_etsi_its_msgs::msg::OriginatingVehicleContainer & ros_message,
  eprosima::fastcdr::Cdr & cdr)
{
  // Member: heading
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.heading,
    cdr);

  // Member: speed
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.speed,
    cdr);

  // Member: has_vehicle_orientation_angle
  cdr << (ros_message.has_vehicle_orientation_angle ? true : false);

  // Member: vehicle_orientation_angle
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.vehicle_orientation_angle,
    cdr);

  // Member: drive_direction
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.drive_direction,
    cdr);

  // Member: has_vehicle_length
  cdr << (ros_message.has_vehicle_length ? true : false);

  // Member: vehicle_length
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.vehicle_length,
    cdr);

  // Member: has_vehicle_width
  cdr << (ros_message.has_vehicle_width ? true : false);

  // Member: vehicle_width
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.vehicle_width,
    cdr);

  // Member: has_vehicle_height
  cdr << (ros_message.has_vehicle_height ? true : false);

  // Member: vehicle_height
  cdr << ros_message.vehicle_height;

  // Member: has_longitudinal_acceleration
  cdr << (ros_message.has_longitudinal_acceleration ? true : false);

  // Member: longitudinal_acceleration
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.longitudinal_acceleration,
    cdr);

  // Member: has_yaw_rate
  cdr << (ros_message.has_yaw_rate ? true : false);

  // Member: yaw_rate
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.yaw_rate,
    cdr);

  return true;
}

bool
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_PUBLIC_ros_etsi_its_msgs
cdr_deserialize(
  eprosima::fastcdr::Cdr & cdr,
  ros_etsi_its_msgs::msg::OriginatingVehicleContainer & ros_message)
{
  // Member: heading
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.heading);

  // Member: speed
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.speed);

  // Member: has_vehicle_orientation_angle
  {
    uint8_t tmp;
    cdr >> tmp;
    ros_message.has_vehicle_orientation_angle = tmp ? true : false;
  }

  // Member: vehicle_orientation_angle
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.vehicle_orientation_angle);

  // Member: drive_direction
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.drive_direction);

  // Member: has_vehicle_length
  {
    uint8_t tmp;
    cdr >> tmp;
    ros_message.has_vehicle_length = tmp ? true : false;
  }

  // Member: vehicle_length
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.vehicle_length);

  // Member: has_vehicle_width
  {
    uint8_t tmp;
    cdr >> tmp;
    ros_message.has_vehicle_width = tmp ? true : false;
  }

  // Member: vehicle_width
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.vehicle_width);

  // Member: has_vehicle_height
  {
    uint8_t tmp;
    cdr >> tmp;
    ros_message.has_vehicle_height = tmp ? true : false;
  }

  // Member: vehicle_height
  cdr >> ros_message.vehicle_height;

  // Member: has_longitudinal_acceleration
  {
    uint8_t tmp;
    cdr >> tmp;
    ros_message.has_longitudinal_acceleration = tmp ? true : false;
  }

  // Member: longitudinal_acceleration
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.longitudinal_acceleration);

  // Member: has_yaw_rate
  {
    uint8_t tmp;
    cdr >> tmp;
    ros_message.has_yaw_rate = tmp ? true : false;
  }

  // Member: yaw_rate
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.yaw_rate);

  return true;
}


size_t
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_PUBLIC_ros_etsi_its_msgs
get_serialized_size(
  const ros_etsi_its_msgs::msg::OriginatingVehicleContainer & ros_message,
  size_t current_alignment)
{
  size_t initial_alignment = current_alignment;

  const size_t padding = 4;
  const size_t wchar_size = 4;
  (void)padding;
  (void)wchar_size;

  // Member: heading
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.heading, current_alignment);

  // Member: speed
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.speed, current_alignment);

  // Member: has_vehicle_orientation_angle
  {
    size_t item_size = sizeof(ros_message.has_vehicle_orientation_angle);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: vehicle_orientation_angle
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.vehicle_orientation_angle, current_alignment);

  // Member: drive_direction
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.drive_direction, current_alignment);

  // Member: has_vehicle_length
  {
    size_t item_size = sizeof(ros_message.has_vehicle_length);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: vehicle_length
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.vehicle_length, current_alignment);

  // Member: has_vehicle_width
  {
    size_t item_size = sizeof(ros_message.has_vehicle_width);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: vehicle_width
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.vehicle_width, current_alignment);

  // Member: has_vehicle_height
  {
    size_t item_size = sizeof(ros_message.has_vehicle_height);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: vehicle_height
  {
    size_t item_size = sizeof(ros_message.vehicle_height);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: has_longitudinal_acceleration
  {
    size_t item_size = sizeof(ros_message.has_longitudinal_acceleration);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: longitudinal_acceleration
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.longitudinal_acceleration, current_alignment);

  // Member: has_yaw_rate
  {
    size_t item_size = sizeof(ros_message.has_yaw_rate);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: yaw_rate
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.yaw_rate, current_alignment);

  return current_alignment - initial_alignment;
}


size_t
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_PUBLIC_ros_etsi_its_msgs
max_serialized_size_OriginatingVehicleContainer(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment)
{
  size_t initial_alignment = current_alignment;

  const size_t padding = 4;
  const size_t wchar_size = 4;
  size_t last_member_size = 0;
  (void)last_member_size;
  (void)padding;
  (void)wchar_size;

  full_bounded = true;
  is_plain = true;

  // Member: heading
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_Heading(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }
  // Member: speed
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_Speed(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }
  // Member: has_vehicle_orientation_angle
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }
  // Member: vehicle_orientation_angle
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_WGS84Angle(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }
  // Member: drive_direction
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_DriveDirection(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }
  // Member: has_vehicle_length
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }
  // Member: vehicle_length
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_VehicleLength(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }
  // Member: has_vehicle_width
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }
  // Member: vehicle_width
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_VehicleWidth(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }
  // Member: has_vehicle_height
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }
  // Member: vehicle_height
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }
  // Member: has_longitudinal_acceleration
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }
  // Member: longitudinal_acceleration
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_LongitudinalAcceleration(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }
  // Member: has_yaw_rate
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }
  // Member: yaw_rate
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_YawRate(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  size_t ret_val = current_alignment - initial_alignment;
  if (is_plain) {
    // All members are plain, and type is not empty.
    // We still need to check that the in-memory alignment
    // is the same as the CDR mandated alignment.
    using DataType = ros_etsi_its_msgs::msg::OriginatingVehicleContainer;
    is_plain =
      (
      offsetof(DataType, yaw_rate) +
      last_member_size
      ) == ret_val;
  }

  return ret_val;
}

bool
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_PUBLIC_ros_etsi_its_msgs
cdr_serialize_key(
  const ros_etsi_its_msgs::msg::OriginatingVehicleContainer & ros_message,
  eprosima::fastcdr::Cdr & cdr)
{
  // Member: heading
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize_key(
    ros_message.heading,
    cdr);

  // Member: speed
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize_key(
    ros_message.speed,
    cdr);

  // Member: has_vehicle_orientation_angle
  cdr << (ros_message.has_vehicle_orientation_angle ? true : false);

  // Member: vehicle_orientation_angle
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize_key(
    ros_message.vehicle_orientation_angle,
    cdr);

  // Member: drive_direction
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize_key(
    ros_message.drive_direction,
    cdr);

  // Member: has_vehicle_length
  cdr << (ros_message.has_vehicle_length ? true : false);

  // Member: vehicle_length
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize_key(
    ros_message.vehicle_length,
    cdr);

  // Member: has_vehicle_width
  cdr << (ros_message.has_vehicle_width ? true : false);

  // Member: vehicle_width
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize_key(
    ros_message.vehicle_width,
    cdr);

  // Member: has_vehicle_height
  cdr << (ros_message.has_vehicle_height ? true : false);

  // Member: vehicle_height
  cdr << ros_message.vehicle_height;

  // Member: has_longitudinal_acceleration
  cdr << (ros_message.has_longitudinal_acceleration ? true : false);

  // Member: longitudinal_acceleration
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize_key(
    ros_message.longitudinal_acceleration,
    cdr);

  // Member: has_yaw_rate
  cdr << (ros_message.has_yaw_rate ? true : false);

  // Member: yaw_rate
  ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize_key(
    ros_message.yaw_rate,
    cdr);

  return true;
}

size_t
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_PUBLIC_ros_etsi_its_msgs
get_serialized_size_key(
  const ros_etsi_its_msgs::msg::OriginatingVehicleContainer & ros_message,
  size_t current_alignment)
{
  size_t initial_alignment = current_alignment;

  const size_t padding = 4;
  const size_t wchar_size = 4;
  (void)padding;
  (void)wchar_size;

  // Member: heading
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size_key(
    ros_message.heading, current_alignment);

  // Member: speed
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size_key(
    ros_message.speed, current_alignment);

  // Member: has_vehicle_orientation_angle
  {
    size_t item_size = sizeof(ros_message.has_vehicle_orientation_angle);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: vehicle_orientation_angle
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size_key(
    ros_message.vehicle_orientation_angle, current_alignment);

  // Member: drive_direction
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size_key(
    ros_message.drive_direction, current_alignment);

  // Member: has_vehicle_length
  {
    size_t item_size = sizeof(ros_message.has_vehicle_length);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: vehicle_length
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size_key(
    ros_message.vehicle_length, current_alignment);

  // Member: has_vehicle_width
  {
    size_t item_size = sizeof(ros_message.has_vehicle_width);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: vehicle_width
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size_key(
    ros_message.vehicle_width, current_alignment);

  // Member: has_vehicle_height
  {
    size_t item_size = sizeof(ros_message.has_vehicle_height);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: vehicle_height
  {
    size_t item_size = sizeof(ros_message.vehicle_height);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: has_longitudinal_acceleration
  {
    size_t item_size = sizeof(ros_message.has_longitudinal_acceleration);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: longitudinal_acceleration
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size_key(
    ros_message.longitudinal_acceleration, current_alignment);

  // Member: has_yaw_rate
  {
    size_t item_size = sizeof(ros_message.has_yaw_rate);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  // Member: yaw_rate
  current_alignment +=
    ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size_key(
    ros_message.yaw_rate, current_alignment);

  return current_alignment - initial_alignment;
}

size_t
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_PUBLIC_ros_etsi_its_msgs
max_serialized_size_key_OriginatingVehicleContainer(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment)
{
  size_t initial_alignment = current_alignment;

  const size_t padding = 4;
  const size_t wchar_size = 4;
  size_t last_member_size = 0;
  (void)last_member_size;
  (void)padding;
  (void)wchar_size;

  full_bounded = true;
  is_plain = true;

  // Member: heading
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_key_Heading(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: speed
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_key_Speed(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: has_vehicle_orientation_angle
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }

  // Member: vehicle_orientation_angle
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_key_WGS84Angle(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: drive_direction
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_key_DriveDirection(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: has_vehicle_length
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }

  // Member: vehicle_length
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_key_VehicleLength(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: has_vehicle_width
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }

  // Member: vehicle_width
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_key_VehicleWidth(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: has_vehicle_height
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }

  // Member: vehicle_height
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }

  // Member: has_longitudinal_acceleration
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }

  // Member: longitudinal_acceleration
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_key_LongitudinalAcceleration(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: has_yaw_rate
  {
    size_t array_size = 1;
    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }

  // Member: yaw_rate
  {
    size_t array_size = 1;
    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_key_YawRate(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  size_t ret_val = current_alignment - initial_alignment;
  if (is_plain) {
    // All members are plain, and type is not empty.
    // We still need to check that the in-memory alignment
    // is the same as the CDR mandated alignment.
    using DataType = ros_etsi_its_msgs::msg::OriginatingVehicleContainer;
    is_plain =
      (
      offsetof(DataType, yaw_rate) +
      last_member_size
      ) == ret_val;
  }

  return ret_val;
}


static bool _OriginatingVehicleContainer__cdr_serialize(
  const void * untyped_ros_message,
  eprosima::fastcdr::Cdr & cdr)
{
  auto typed_message =
    static_cast<const ros_etsi_its_msgs::msg::OriginatingVehicleContainer *>(
    untyped_ros_message);
  return cdr_serialize(*typed_message, cdr);
}

static bool _OriginatingVehicleContainer__cdr_deserialize(
  eprosima::fastcdr::Cdr & cdr,
  void * untyped_ros_message)
{
  auto typed_message =
    static_cast<ros_etsi_its_msgs::msg::OriginatingVehicleContainer *>(
    untyped_ros_message);
  return cdr_deserialize(cdr, *typed_message);
}

static uint32_t _OriginatingVehicleContainer__get_serialized_size(
  const void * untyped_ros_message)
{
  auto typed_message =
    static_cast<const ros_etsi_its_msgs::msg::OriginatingVehicleContainer *>(
    untyped_ros_message);
  return static_cast<uint32_t>(get_serialized_size(*typed_message, 0));
}

static size_t _OriginatingVehicleContainer__max_serialized_size(char & bounds_info)
{
  bool full_bounded;
  bool is_plain;
  size_t ret_val;

  ret_val = max_serialized_size_OriginatingVehicleContainer(full_bounded, is_plain, 0);

  bounds_info =
    is_plain ? ROSIDL_TYPESUPPORT_FASTRTPS_PLAIN_TYPE :
    full_bounded ? ROSIDL_TYPESUPPORT_FASTRTPS_BOUNDED_TYPE : ROSIDL_TYPESUPPORT_FASTRTPS_UNBOUNDED_TYPE;
  return ret_val;
}

static message_type_support_callbacks_t _OriginatingVehicleContainer__callbacks = {
  "ros_etsi_its_msgs::msg",
  "OriginatingVehicleContainer",
  _OriginatingVehicleContainer__cdr_serialize,
  _OriginatingVehicleContainer__cdr_deserialize,
  _OriginatingVehicleContainer__get_serialized_size,
  _OriginatingVehicleContainer__max_serialized_size,
  nullptr
};

static rosidl_message_type_support_t _OriginatingVehicleContainer__handle = {
  rosidl_typesupport_fastrtps_cpp::typesupport_identifier,
  &_OriginatingVehicleContainer__callbacks,
  get_message_typesupport_handle_function,
  &ros_etsi_its_msgs__msg__OriginatingVehicleContainer__get_type_hash,
  &ros_etsi_its_msgs__msg__OriginatingVehicleContainer__get_type_description,
  &ros_etsi_its_msgs__msg__OriginatingVehicleContainer__get_type_description_sources,
};

}  // namespace typesupport_fastrtps_cpp

}  // namespace msg

}  // namespace ros_etsi_its_msgs

namespace rosidl_typesupport_fastrtps_cpp
{

template<>
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_EXPORT_ros_etsi_its_msgs
const rosidl_message_type_support_t *
get_message_type_support_handle<ros_etsi_its_msgs::msg::OriginatingVehicleContainer>()
{
  return &ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::_OriginatingVehicleContainer__handle;
}

}  // namespace rosidl_typesupport_fastrtps_cpp

#ifdef __cplusplus
extern "C"
{
#endif

const rosidl_message_type_support_t *
ROSIDL_TYPESUPPORT_INTERFACE__MESSAGE_SYMBOL_NAME(rosidl_typesupport_fastrtps_cpp, ros_etsi_its_msgs, msg, OriginatingVehicleContainer)() {
  return &ros_etsi_its_msgs::msg::typesupport_fastrtps_cpp::_OriginatingVehicleContainer__handle;
}

#ifdef __cplusplus
}
#endif
